{
  "name": "finfolio-server",
  "version": "1.0.0",
  "private": true,
  "description": "FinFolio 後端 — 認證(bcrypt + JWT)、跟單設定持久化、Decimal P&L",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.8.1",
    "cors": "^2.8.5",
    "decimal.js": "^10.6.0",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2"
  }
}
